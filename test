import React from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Mail } from "lucide-react";

export default function Homepage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-blue-50 to-white text-gray-800">
      {/* Header */}
      <header className="py-6 px-4 md:px-12 flex justify-between items-center border-b border-blue-100">
        <h1 className="text-2xl font-bold text-blue-700">LaunchSpace</h1>
        <nav className="space-x-4">
          <Button variant="ghost">Learn More</Button>
          <Button variant="ghost">Blog</Button>
          <Button>Get Started</Button>
        </nav>
      </header>

      {/* Hero Section */}
      <section className="text-center py-20 px-6 md:px-20 bg-white">
        <h2 className="text-4xl md:text-5xl font-bold mb-4 text-blue-800">
          Your AI Startup Starts Here
        </h2>
        <p className="text-lg md:text-xl max-w-2xl mx-auto mb-8 text-gray-600">
          LaunchSpace is your friendly guide to building serious tech and AI businesses from the ground up. No experience? No problem.
        </p>
        <div className="flex flex-col md:flex-row justify-center gap-4">
          <Button size="lg">Get Started</Button>
          <Button size="lg" variant="outline">
            Explore Resources
          </Button>
        </div>
      </section>

      {/* What We Offer Section */}
      <section className="py-16 px-6 md:px-20 bg-blue-50">
        <div className="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          <Card>
            <CardContent className="p-6">
              <h3 className="text-xl font-semibold text-blue-700 mb-2">
                Step-by-Step Guidance
              </h3>
              <p className="text-gray-600">
                From idea to launch, we help you every step of the way with clear, beginner-friendly lessons.
              </p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-6">
              <h3 className="text-xl font-semibold text-blue-700 mb-2">
                AI Tools & Templates
              </h3>
              <p className="text-gray-600">
                Get access to plug-and-play tools to help you build faster—no coding required.
              </p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-6">
              <h3 className="text-xl font-semibold text-blue-700 mb-2">
                Community Support
              </h3>
              <p className="text-gray-600">
                You're not alone—join other founders on the same journey and get feedback, tips, and encouragement.
              </p>
            </CardContent>
          </Card>
        </div>
      </section>

      {/* Newsletter CTA */}
      <section className="py-16 px-6 md:px-20 bg-white text-center">
        <h3 className="text-2xl font-semibold text-blue-800 mb-4">
          Stay in the Loop
        </h3>
        <p className="text-gray-600 mb-6">
          Get updates on new tools, tutorials, and exclusive tips straight to your inbox.
        </p>
        <form className="flex flex-col md:flex-row justify-center items-center gap-4 max-w-lg mx-auto">
          <Input type="email" placeholder="Enter your email" className="w-full md:flex-1" />
          <Button size="lg">
            <Mail className="w-4 h-4 mr-2" /> Subscribe
          </Button>
        </form>
      </section>

      {/* Footer */}
      <footer className="py-8 px-6 md:px-20 bg-blue-900 text-white">
        <div className="flex flex-col md:flex-row justify-between items-center gap-4">
          <p>&copy; {new Date().getFullYear()} LaunchSpace. All rights reserved.</p>
          <nav className="space-x-4">
            <a href="#" className="hover:underline">Resources</a>
            <a href="#" className="hover:underline">Blog</a>
            <a href="#" className="hover:underline">Contact</a>
          </nav>
        </div>
      </footer>
    </div>
  );
}
